import { useState } from "react";

export default function Home() {
  const [page, setPage] = useState("home");

  return (
    <div style={{ minHeight: "100vh", background: "#020617", color: "white", padding: "40px" }}>
      <nav style={{ display: "flex", gap: "20px", marginBottom: "40px" }}>
        <button onClick={() => setPage("home")}>Home</button>
        <button onClick={() => setPage("about")}>About</button>
        <button onClick={() => setPage("services")}>Services</button>
        <button onClick={() => setPage("booking")}>Book</button>
        <button onClick={() => setPage("contact")}>Contact</button>
      </nav>

      {page === "home" && (
        <>
          <h1>Ingle Financial Services</h1>
          <p>Professional financial consultations, assistance, and quoting services you can trust.</p>
        </>
      )}

      {page === "about" && (
        <>
          <h2>About Us</h2>
          <p>
            Ingle Financial Services was founded to provide honest, transparent, and accessible
            financial guidance. We focus on helping individuals make confident financial decisions
            through personalized support.
          </p>
        </>
      )}

      {page === "services" && (
        <>
          <h2>Our Services</h2>
          <ul>
            <li>Financial Consultations</li>
            <li>Quoting Services</li>
            <li>Budget & Planning Assistance</li>
            <li>General Financial Support</li>
          </ul>
        </>
      )}

      {page === "booking" && (
        <>
          <h2>Book a Consultation</h2>
          <input placeholder="Name" /><br />
          <input placeholder="Email" /><br />
          <input placeholder="Phone" /><br />
          <textarea placeholder="Describe your needs" /><br />
          <button>Submit</button>
        </>
      )}

      {page === "contact" && (
        <>
          <h2>Contact</h2>
          <p>Phone: 423-506-5549</p>
          <p>Email: gavin4726@icloud.com</p>
        </>
      )}
    </div>
  );
}
